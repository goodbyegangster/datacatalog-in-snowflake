# アプリのテーマ設定。標準カラーは Snowflake コーポレートカラー（水色）に寄せる。
# ローカル / SiS 双方でテーマとして参照される。

[theme]
base = "light"
primaryColor = "#29B5E8"         # Snowflake blue
backgroundColor = "#f5f7f794"    # 背景色

[server]
runOnSave = true
