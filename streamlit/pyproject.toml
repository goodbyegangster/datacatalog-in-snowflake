[project]
name = "datacatalog"
version = "1.0.0"
description = "Streamlit App in Snowflake"
requires-python = "~=3.11.0"

dependencies = [
  "streamlit[snowflake]~=1.59.0",
  "jinja2~=3.1.0",
]

[dependency-groups]
dev = [
  "pandas-stubs>=3.0.3.260530",
  "pytest~=9.0",
]
