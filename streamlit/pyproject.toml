[project]
name = "datacatalog"
version = "1.0.0"
description = "Streamlit App in Snowflake"
requires-python = "~=3.11.0"

dependencies = [
  "streamlit[snowflake]~=1.58.0",
]

[dependency-groups]
dev = [
  "pytest~=9.0",
]
